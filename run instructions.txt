Cd to the current project directory
cd C:\Users\david\OneDrive\Documents\GitHub\258MONAI

Activate venv
python -m venv .venv
.\.venv\Scripts\activate

Install pip if you haven’t already
python -m pip install --upgrade pip

Install dependencies
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118

pip install monai nibabel
pip install matplotlib

Run Dataset preprocessing
python dataset.py
pip install tensorboard (Optional to see the loading/progress)

Uses the dataloaders defined in dataset.py to train the 3D UNet.
python train_baseline.py
Loads models/best_metric_model.pth. Runs MC Dropout (multiple stochastic passes) on validation volumes.
python infer_mc_dropout.py 
Loads mc_outputs/val_case0_mc_dropout.pt. Plots a single axial slice (center slice). Shows a window with three images (if a GUI backend is available).
python visualize_mc_dropout.py 
Collects all voxel probabilities and labels. Computes bin-wise accuracy, reliability curve, and ECE. Generates reliability diagram
python calibration_reliability.py 


Run the demo Jupyter file for the model evaluation
